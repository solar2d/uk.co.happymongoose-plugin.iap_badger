Sources for the plugin `plugin.iap_badger`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.iap_badger" = {
            publisherId = "uk.co.happymongoose",
        },
    },
}
```
